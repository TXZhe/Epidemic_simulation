#pragma once

#define USRNUM 10146
//#define USRNUM 10
#define TEST_USRNUM 10130
#define STEP_LEN 180				//the TIME WINDOW of updating the network and do the epidemic simulation
#define START_INI 108438

#define max(a,b) (((a) > (b)) ? (a) : (b))
#define min(a,b) (((a) < (b)) ? (a) : (b))

typedef std::pair<int, int> USERS;
